# 方向マーカー配置アルゴリズム仕様書 📍

## 📝 このドキュメントについて

このドキュメントは、方向マーカー配置アルゴリズムの仕様を定義するものです。

### 仕様書作成の原則 📜

1. **言語非依存の記述**：
   - 仕様書には特定のプログラミング言語のコードを直接記載しないこと
   - アルゴリズムや処理は、言語に依存しない日本語の説明文で記述すること
   - これにより、どのプログラミング言語でも実装可能な汎用的な仕様となる

2. **概念と原則の優先**：
   - 実装の詳細よりも、基本概念と原則を明確に記述すること
   - 「なぜそうするのか」という理由を含めて説明すること

3. **構造化された説明**：
   - 処理手順は段階的に、番号付きリストで説明すること
   - 重要な要件は太字で強調すること

4. **設計パターンの明確化**：
   - 関数の責任分離と再利用性を重視した設計原則を明記すること
   - データ取得と座標処理の分離について説明すること

### 開発者への指示

1. **仕様書への準拠**：
   - このドキュメントに記載されたアルゴリズムと実装方法に従って開発を行ってください
   - 特に南北方向（N・S）の分離については、記載された垂直投影アルゴリズムを厳守してください
   - 実装時に不明点がある場合は、まずこの仕様書を参照してください

2. **設計原則の遵守**：
   - **関数の責任分離**：データ取得と座標処理は分離して実装すること
   - **再利用性の向上**：座標データを直接受け取る関数設計を採用すること
   - **命名の一貫性**：変数名は統一感を持って命名すること（coordinates、corner_positions等）

3. **仕様書の問題点の指摘**：
   - この仕様書自体に問題点や改善点を発見した場合は、遠慮なく指摘してください
   - 指摘の際は具体的な問題点と、可能であれば修正案を提示してください
   - 特に方向マーカーが正しく分離されない場合や、予期しない位置に配置される場合は、すぐに報告してください

4. **改善提案**：
   - アルゴリズムの効率化や精度向上のための提案も歓迎します
   - 提案は「〜の部分を〜のように変更することで、〜の問題が解決できる」という形式で具体的に記述してください

### 変更履歴

- 2024-08-05: 初版作成
- 2024-08-05: 南側（S）の配置要件を明確化 - SW-SE間のポリゴン線上に配置することを明記
- 2024-08-05: ポリゴンの実形状の尊重を基本要件に追加 - 対角点を直線で結んだ線上ではなく、ポリゴンの実際の辺上にマーカーを配置する原則を明確化
- 2024-08-05: ポリゴン辺取得アルゴリズムを追加 - `find_polygon_edge_between_points`関数の仕様を追加
- 2024-08-05: 仕様書のコードを日本語の説明に置き換え、より理解しやすくした
- 2024-08-05: 仕様書作成の原則を追加 - 言語非依存の記述、概念と原則の優先、構造化された説明
- 2024-08-05: **設計原則の更新** - 関数の責任分離、再利用性向上、命名の一貫性を追加
- 2024-08-05: **関数名の統一** - coordinates、corner_positions等の統一命名規則を反映
- 2024-08-05: **新しい関数設計** - get_place_geodataframe、get_location_by_direction等の新しい関数名と設計パターンを反映
- 2024-08-05: **loggingシステムの実装** - 環境変数によるログレベル制御とファイル出力機能を追加
- 2024-08-05: **ファイル名の統一** - map_extensions_osmnx.pyへの変更と出力ファイル名の統一
- 2024-08-05: **改善提案セクションの追加** - 現在の実装状況と今後の改善提案を詳細に記載
- [ここに今後の変更履歴を記録]

## 🎯 目的

地図上で場所の輪郭（ポリゴン）から方向マーカー（N・E・S・W、NE・NW・SE・SW）を正確に配置するためのアルゴリズム仕様です。このドキュメントは、`map_extensions_osmnx.py` で実装されている方向マーカー配置ロジックの要件と実装詳細を説明します。

## 🏗️ 設計原則

### 1. 関数の責任分離

**データ取得と座標処理の分離**：
- **データ取得関数**：場所名からGeoDataFrameを取得する責任のみを持つ
- **座標処理関数**：座標リストを直接受け取り、処理を行う責任を持つ
- **可視化関数**：座標データを受け取り、表示のみを行う責任を持つ

### 2. 再利用性の向上

**座標データの直接受け渡し**：
- 関数間の依存関係を最小限に抑える
- 座標データを直接受け取ることで、テストしやすく、柔軟性を向上
- 中間データ構造（GeoDataFrame）への依存を減らす

### 3. 命名の一貫性

**統一された命名規則**：
- `coordinates`：座標リスト
- `corner_positions`：角位置データ
- `rotated_coordinates`：回転後の座標
- `original_coordinates`：元の座標

## 📊 方向マーカーの配置要件

### 基本要件

1. **8方向のマーカー配置**：
   - 4つの主要方向（N・E・S・W）
   - 4つの対角方向（NE・NW・SE・SW）

2. **マーカー位置の条件**：
   - **対角方向（NE・NW・SE・SW）**：ポリゴンの外接矩形の四隅に配置
   - **主要方向（N・E・S・W）**：対応する辺の中央付近でポリゴン線上に配置
     - **N**：NW-NE間のポリゴン線上（北側の辺）
     - **E**：NE-SE間のポリゴン線上（東側の辺）
     - **S**：SW-SE間のポリゴン線上（南側の辺）- 重要：SE-NE間ではなく
     - **W**：NW-SW間のポリゴン線上（西側の辺）

3. **マーカーの重複防止**：
   - 各マーカーは明確に分離されていること
   - 特にN/S、E/Wが重複しないこと

4. **ポリゴンの実形状の尊重**：
   - 主要方向（N・E・S・W）の配置は、対角点を直線で結んだ線上ではなく、**ポリゴンの実際の辺上**に配置すること
   - ポリゴンの形状が複雑な場合でも、実際の辺の形状に沿ってマーカーを配置すること
   - 実際の辺が複数の点で構成される場合は、その辺の中点を使用すること

## 🔧 実装アルゴリズム

### 1. データ取得層

#### 場所データの取得
- **関数名**：`get_place_geodataframe(place)`
- **責任**：場所名からGeoDataFrameを取得
- **戻り値**：GeoDataFrame（場所の地理データ）
- **処理内容**：
  1. OSMnxを使用して場所の地理データを取得
  2. データの存在確認とエラーハンドリング
  3. GeoDataFrameを返却

#### 座標抽出
- **処理内容**：
  1. GeoDataFrameからジオメトリを取得
  2. ポリゴンの外縁座標をリスト形式で抽出
  3. 座標リストを後続の処理関数に渡す

### 2. 座標処理層

#### 形状分析と回転
- **関数名**：`determine_shape_orientation(coordinates)`
- **責任**：座標リストから最適な回転角度を計算
- **入力**：座標リスト `[(lon, lat), ...]`
- **戻り値**：最適な回転角度（ラジアン）

#### 座標回転
- **関数名**：`rotate_coordinates(coordinates, angle)`
- **責任**：座標リストを指定角度で回転
- **入力**：座標リスト、回転角度
- **戻り値**：回転後の座標リスト

#### 角位置計算
- **関数名**：`calculate_corner_positions(rotated_coordinates, center_lon, center_lat)`
- **責任**：回転後の座標系で角位置を計算
- **入力**：回転後の座標、中心座標
- **戻り値**：角位置の辞書

### 3. 方向別位置計算層

#### 方向別辺上位置計算
- **関数名**：`calculate_direction_edge_position(coordinates, direction)`
- **責任**：座標リストから指定方向の辺上位置を計算
- **入力**：座標リスト、方向（"東", "西", "南", "北", "北東", "北西", "南東", "南西"）
- **戻り値**：辺上の位置座標 `(lon, lat)`

#### 方向別位置取得
- **関数名**：`get_location_by_direction(coordinates, direction)`
- **責任**：座標リストから指定方向の位置を取得（エラーハンドリング含む）
- **入力**：座標リスト、方向
- **戻り値**：位置座標またはNone

### 4. 座標変換層

#### 回転座標の元座標系への変換
- **関数名**：`convert_rotated_to_original(rotated_corner_positions, angle, original_coordinates=None)`
- **責任**：回転後の座標を元の座標系に戻す
- **入力**：回転後の角位置、回転角度、元の座標（オプション）
- **戻り値**：元の座標系での角位置辞書

### 5. 可視化層

#### 回転後座標の可視化
- **関数名**：`visualize_rotated_positions(rotated_coordinates, optimal_angle)`
- **責任**：回転後の座標を可視化
- **入力**：回転後の座標、最適角度
- **戻り値**：角位置辞書

#### 方向位置の可視化
- **関数名**：`visualize_with_direction_positions(corner_positions, coordinates)`
- **責任**：方向位置を可視化
- **入力**：角位置辞書、座標リスト
- **戻り値**：なし（ファイル出力）

## 🔧 詳細実装アルゴリズム

### 1. 対角方向（NE・NW・SE・SW）の決定

対角方向（NE・NW・SE・SW）の位置は、ポリゴンの外接矩形の四隅に配置します。具体的な手順は以下の通りです：

1. **回転後の座標系でポリゴンの中心点を計算**
   - ポリゴンの全座標の平均値を中心点とする

2. **各象限ごとに最も外側にある点を選択**
   - **北東（NE）**: 中心点より右上（X座標が大きく、Y座標が大きい）の領域で、最もY座標が大きい点
   - **北西（NW）**: 中心点より左上（X座標が小さく、Y座標が大きい）の領域で、最もY座標が大きい点
   - **南東（SE）**: 中心点より右下（X座標が大きく、Y座標が小さい）の領域で、最もX座標が大きい点
   - **南西（SW）**: 中心点より左下（X座標が小さく、Y座標が小さい）の領域で、最もX座標が小さい点

3. **選択基準**
   - 北側（NE・NW）: Y座標の最大値を優先
   - 南側（SE・SW）: X座標の最大値または最小値を優先

### 2. 主要方向（N・E・S・W）の決定

#### 東西方向（E・W）

東側（E）と西側（W）の位置は、ポリゴンの実際の辺上に配置します。具体的な手順は以下の通りです：

##### 東側（E）の配置

1. **NE-SE間のポリゴン辺を特定**
   - 北東（NE）と南東（SE）の角を結ぶポリゴン上の実際の辺を取得

2. **辺の中点を計算**
   - ポリゴン辺が複数の点で構成される場合：
     - 偶数個の点の場合：中央の2点の中間点を使用
     - 奇数個の点の場合：中央の点をそのまま使用

3. **フォールバック処理**
   - ポリゴン辺が見つからない場合：
     - NE-SE間を直線で結んだ線分の中点を計算
     - 必要に応じて垂直投影を適用

##### 西側（W）の配置

1. **NW-SW間のポリゴン辺を特定**
   - 北西（NW）と南西（SW）の角を結ぶポリゴン上の実際の辺を取得

2. **辺の中点を計算**
   - ポリゴン辺が複数の点で構成される場合：
     - 偶数個の点の場合：中央の2点の中間点を使用
     - 奇数個の点の場合：中央の点をそのまま使用

3. **フォールバック処理**
   - ポリゴン辺が見つからない場合：
     - NW-SW間を直線で結んだ線分の中点を計算
     - 必要に応じて垂直投影を適用

#### 南北方向（N・S）

北側（N）と南側（S）の位置は、ポリゴンの実際の辺上に配置します。具体的な手順は以下の通りです：

##### 北側（N）の配置

1. **NW-NE間のポリゴン辺を特定**
   - 北西（NW）と北東（NE）の角を結ぶポリゴン上の実際の辺を取得

2. **辺の中点を計算**
   - ポリゴン辺が複数の点で構成される場合：
     - 偶数個の点の場合：中央の2点の中間点を使用
     - 奇数個の点の場合：中央の点をそのまま使用

3. **フォールバック処理**
   - ポリゴン辺が見つからない場合：
     - NW-NE間を直線で結んだ線分の中点を計算
     - 中点から垂直線を引き、ポリゴンとの交点を計算（北向き）
     - 交点が見つからない場合は中点を使用

##### 南側（S）の配置

1. **SW-SE間のポリゴン辺を特定**
   - 南西（SW）と南東（SE）の角を結ぶポリゴン上の実際の辺を取得
   - **重要**: Sは必ずSW-SE間のポリゴン線上に配置すること
   - EとSが重複しないよう、SE-NE間ではなくSW-SE間を使用する

2. **辺の中点を計算**
   - ポリゴン辺が複数の点で構成される場合：
     - 偶数個の点の場合：中央の2点の中間点を使用
     - 奇数個の点の場合：中央の点をそのまま使用

3. **フォールバック処理**
   - ポリゴン辺が見つからない場合：
     - SW-SE間を直線で結んだ線分の中点を計算
     - 中点から垂直線を引き、ポリゴンとの交点を計算（南向き）
     - 交点が見つからない場合は中点を使用

### 3. ポリゴン辺取得アルゴリズム

ポリゴン上の2点間の実際の辺を取得するアルゴリズムです。この処理は、直線で結んだ線分ではなく、ポリゴンの実際の形状に沿った辺を取得するために重要です。

#### 処理手順

1. **ポリゴン上の2点のインデックスを特定**
   - 与えられた2点（point1, point2）がポリゴン上のどの頂点に対応するかを特定
   - 許容誤差（epsilon）を設定して、浮動小数点の誤差を考慮

2. **点が見つからない場合の処理**
   - どちらかの点がポリゴン上に見つからない場合は空のリストを返す

3. **ポリゴン上の辺を構成する点列の取得**
   - ポリゴンは閉じた形状であることを考慮（最初の点と最後の点は繋がっている）
   - 時計回りと反時計回りの両方の経路を考慮

4. **最短経路の選択**
   - 時計回りと反時計回りの経路を比較し、点の数が少ない方を選択
   - 選択した経路が反時計回りの場合は、点の順序を反転

5. **結果の返却**
   - ポリゴン上のpoint1とpoint2の間の辺を構成する点のリストを返す
   - この点列を使用して、辺の中点や形状を正確に把握できる

### 4. 垂直投影アルゴリズム

垂直投影アルゴリズムは、線分の中点からポリゴンの辺への垂直投影を計算するための処理です。特に南北方向（N・S）のマーカー配置で重要です。

#### 処理手順

1. **方向に応じた処理分岐**
   - **東西方向（E・W）の場合**：
     - 単純に線分の中点を返す（垂直投影は行わない）
   
   - **南北方向（N・S）の場合**：
     - 線分の中点を計算
     - 線分に垂直な方向ベクトルを計算
     - 方向に応じて垂直ベクトルを調整

2. **北向き垂直ベクトルの計算**
   - 線分ベクトルを90度回転（時計回り）
   - ベクトルの長さを正規化
   - Y方向の係数を大きく（0.005）、X方向の係数を小さく（0.001）設定して北向きを強調

3. **南向き垂直ベクトルの計算**
   - 線分ベクトルを-90度回転（反時計回り）
   - ベクトルの長さを正規化
   - Y方向の係数を大きく（0.005）、X方向の係数を小さく（0.001）設定して南向きを強調

4. **垂直線とポリゴンの交点を計算**
   - 中点から垂直ベクトル方向に線を引き、ポリゴンとの交点を探索
   - 交点が見つかった場合はその点を返す

5. **フォールバック処理**
   - 交点が見つからない場合は、方向に応じた代替処理を実行
   
   - **北側の場合**：
     - Y座標が大きい順に上位5点を抽出
     - 中点のX座標に近い点を選択
   
   - **南側の場合**：
     - Y座標が小さい順に上位5点を抽出
     - 中点のX座標に近い点を選択
   
   - いずれも見つからない場合は中点を返す

## 🚨 実装上の注意点

1. **方向ベクトルの強調**：
   - 北向きと南向きのベクトルは明確に分離する必要がある
   - Y方向の係数を大きく（0.005）、X方向の係数を小さく（0.001）設定して南北方向を強調

2. **交点が見つからない場合の対応**：
   - 北側はY座標が大きい点を優先
   - 南側はY座標が小さい点を優先
   - X座標が中点に近い点を選択する

3. **重複防止**：
   - 南北方向は特に重複しやすいため、垂直ベクトルの方向を明確に分ける
   - 交点が見つからない場合は方向に応じた代替アルゴリズムを使用

4. **関数設計の原則**：
   - データ取得と座標処理は分離する
   - 座標データを直接受け取る関数設計を採用する
   - 命名は一貫性を保つ（coordinates、corner_positions等）

## 📊 期待される結果

1. **対角方向（NE・NW・SE・SW）**：
   - ポリゴンの外接矩形の四隅に配置される
   - 各象限に1つずつ、明確に分離されている

2. **主要方向（N・E・S・W）**：
   - 対応する辺の中央付近でポリゴン線上に配置される
   - 特にN/S、E/Wは明確に分離されている
   - 東西方向は単純な中点、南北方向は垂直投影による交点

3. **設計品質**：
   - 関数の責任が明確に分離されている
   - 再利用性が高く、テストしやすい設計
   - 命名が一貫性を持っている

## 🛠️ デバッグ方法

問題が発生した場合は、以下の点を確認してください：

1. **座標の出力**：
   ```python
   print(f"🔍 デバッグ: 東 = ({corner_positions['東'][0]:.6f}, {corner_positions['東'][1]:.6f})")
   print(f"🔍 デバッグ: 西 = ({corner_positions['西'][0]:.6f}, {corner_positions['西'][1]:.6f})")
   print(f"🔍 デバッグ: 北 = ({corner_positions['北'][0]:.6f}, {corner_positions['北'][1]:.6f})")
   print(f"🔍 デバッグ: 南 = ({corner_positions['南'][0]:.6f}, {corner_positions['南'][1]:.6f})")
   ```

2. **垂直投影の確認**：
   ```python
   print(f"🔍 垂直投影デバッグ: 角1={corner1}, 角2={corner2}, 方向={direction_hint}")
   print(f"🔍 南北方向：中点={midpoint}, 垂直ベクトル={perpendicular_vector}")
   ```

3. **可視化範囲の確認**：
   ```python
   print(f"🔍 デバッグ: 可視化範囲 - X: {x_min:.6f} ～ {x_max:.6f}, Y: {y_min:.6f} ～ {y_max:.6f}")
   ```

4. **関数設計の確認**：
   - データ取得と座標処理が適切に分離されているか
   - 座標データを直接受け取る関数設計になっているか
   - 命名が一貫性を持っているか

## 📝 まとめ

この方向マーカー配置アルゴリズムは、地図上のポリゴンから8方向（N・E・S・W・NE・NW・SE・SW）のマーカーを正確に配置します。特に南北方向（N・S）は垂直投影による交点計算と、方向に応じた代替アルゴリズムを組み合わせることで、重複を防止し明確に分離された配置を実現しています。

**設計面では**、関数の責任分離、再利用性の向上、命名の一貫性を重視した設計原則を採用し、保守性と拡張性に優れたアルゴリズムとなっています。

## 🔄 フィードバックと改善提案

### 現在の実装状況と改善提案

#### 🎯 実装済み機能

1. **loggingシステム**:
   - ✅ 環境変数によるログレベル制御（DEBUG/INFO/WARNING/ERROR）
   - ✅ コンソールとファイルの両方にログ出力
   - ✅ 日本語での詳細なログメッセージ

2. **ファイル名の統一**:
   - ✅ `map_extensions_osmnx.py`への変更
   - ✅ 出力ファイル名の統一（`map_extensions_osmnx_*.png`）
   - ✅ ログファイル名の統一（`map_extensions_osmnx.log`）

3. **方向マーカー配置アルゴリズム**:
   - ✅ 8方向（N・E・S・W・NE・NW・SE・SW）の配置
   - ✅ 人間の形状認識に基づくアルゴリズム
   - ✅ 垂直投影による南北方向の分離

#### 🚀 改善提案（優先度順）

##### 高優先度 🔴

1. **エラーハンドリングの強化**:
   - **現状**: `get_geodataframe_address`でNominatim geocoderエラーが発生
   - **提案**: 複数のgeocodingサービス（Google Maps API、Bing Maps等）のフォールバック機能
   - **期待効果**: データ取得の成功率向上

2. **パフォーマンス最適化**:
   - **現状**: 毎回OSMnxからデータを取得
   - **提案**: キャッシュ機能の実装（既に`api_cache.db`が存在）
   - **期待効果**: 実行速度の大幅向上

3. **設定ファイルの導入**:
   - **現状**: ハードコードされた設定値
   - **提案**: `config.yaml`による設定管理
   - **期待効果**: 環境別設定の柔軟性向上

##### 中優先度 🟡

4. **テストカバレッジの向上**:
   - **現状**: 基本的な動作確認のみ
   - **提案**: 単体テスト、統合テストの追加
   - **期待効果**: 品質保証と保守性向上

5. **ドキュメントの充実**:
   - **現状**: 基本的な仕様書のみ
   - **提案**: API仕様書、ユーザーガイドの追加
   - **期待効果**: 開発者とユーザーの理解向上

6. **可視化機能の拡張**:
   - **現状**: 基本的なプロット機能
   - **提案**: インタラクティブな可視化（Plotly等）
   - **期待効果**: デバッグと分析の効率化

##### 低優先度 🟢

7. **Web API化**:
   - **現状**: コマンドライン実行のみ
   - **提案**: FastAPI等によるWeb API実装
   - **期待効果**: 他システムとの連携可能性

8. **機械学習の導入**:
   - **現状**: ルールベースのアルゴリズム
   - **提案**: 形状認識の精度向上のためのMLモデル
   - **期待効果**: より正確な方向マーカー配置

#### 📊 実装ロードマップ

##### Phase 1（1-2週間）: 安定性向上
- エラーハンドリングの強化
- キャッシュ機能の実装
- 基本的なテスト追加

##### Phase 2（2-3週間）: 機能拡張
- 設定ファイルの導入
- 可視化機能の拡張
- ドキュメントの充実

##### Phase 3（1-2ヶ月）: 高度な機能
- Web API化
- 機械学習の導入
- パフォーマンス最適化

#### 🛠️ 技術的改善提案

1. **依存関係の最適化**:
   ```yaml
   # requirements.txtの改善
   osmnx>=1.0.0
   geopandas>=0.12.0
   matplotlib>=3.5.0
   numpy>=1.21.0
   scipy>=1.7.0
   plotly>=5.0.0  # 新規追加
   fastapi>=0.68.0  # 新規追加
   ```

2. **コード構造の改善**:
   ```python
   # クラスベースの設計への移行
   class MapExtensionsOSMnx:
       def __init__(self, config):
           self.config = config
           self.cache = Cache()
   
       def get_direction_markers(self, place):
           # 実装
   ```

3. **設定管理の改善**:
   ```yaml
   # config.yaml
   logging:
     level: INFO
     file: map_extensions_osmnx.log
   
   geocoding:
     primary: osmnx
     fallback: google_maps
   
   visualization:
     output_format: png
     dpi: 150
   ```

### フィードバックの提出方法

仕様書の問題点や改善提案がある場合は、以下のいずれかの方法でフィードバックを提出してください：

1. **GitHub Issue**: 
   - リポジトリの Issues セクションに新しい Issue を作成
   - タイトルに「[方向マーカー仕様書] 〜について」と記載
   - 問題点や改善提案を具体的に記述

2. **プルリクエスト**:
   - 修正案がある場合は、変更を含むプルリクエストを作成
   - 変更内容と理由を明記

3. **直接コミュニケーション**:
   - チーム内の定例ミーティングで議題として提案
   - 緊急性が高い場合はチャットツールで共有

### フィードバック時に含めるべき情報

1. **問題の具体的な説明**:
   - どのような状況で問題が発生するか
   - 期待される動作と実際の動作の違い
   - 再現手順（可能であれば）

2. **改善提案の場合**:
   - 現在の実装の課題点
   - 提案する変更内容
   - 期待される改善効果

3. **視覚資料**:
   - 問題を示すスクリーンショットや図
   - 修正案を説明する図やコード例

### フィードバック対応プロセス

1. フィードバックを受け取った後、チーム内で検討
2. 仕様書の更新が必要な場合は、変更履歴に記録
3. 実装の変更が必要な場合は、コードも同時に更新
4. 変更後、テストを実施して効果を確認

皆様からのフィードバックを歓迎し、より良いアルゴリズムの実現に向けて協力していきましょう。